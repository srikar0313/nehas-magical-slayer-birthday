import { GiftCard } from '@/components/GiftCard';

const Index = () => {
  return (
    <div className="relative">
      {/* Gift Card */}
      <GiftCard />
    </div>
  );
};

export default Index;
